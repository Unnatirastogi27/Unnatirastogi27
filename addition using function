#include <stdio.h>
int add(int,int);
int main() {
    int num1,num2;
    num1=20;
    num2=40;
    int sum=add(num1,num2);
    printf("%d",sum);
    return 0;
}
int add(int m, int n)
{
    return(m+n);
}
